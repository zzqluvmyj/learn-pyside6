# backspace del
# cursorPosition cursorPositionAt cursorBackward cursorForward end home
# deselect setSelection selectAll
# cursorWordBackward cursorWordForward
# setSelection hasSelectedText selectedText selectionEnd() selectionStart() selectionLength().
# insert
# setModified
# isRedoAvailable isUndoAvailable

# displayText()保存显示的文本。
# 如果echoMode为Normal，则返回与text () 相同的值；
# 如果EchoMode是Password或PasswordEchoOnEdit它返回一个平台相关的密码掩码字符串text ().length() 大小，例如“******”；
# 如果EchoMode为NoEcho ，则返回一个空字符串“”。
# 默认情况下，此属性包含一个空字符串。
